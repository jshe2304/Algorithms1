package week3;

import java.util.Arrays;

public class test {

    public static void main (String[] args) {


        /*
        Point[] points = new Point[] {new Point(0, 0), new Point(0, 1), new Point(1, 2), new Point(1, 1), new Point(2, 1), new Point(2, 0), new Point(2, 2), new Point(3, 3)};

        for (Point point : points) {
            System.out.print(point);
        }
        System.out.println();

        for (Point point : points) {
            System.out.println(point);
            Arrays.sort(points);
            Arrays.sort(points, point.slopeOrder());
            for (Point p : points) {
                System.out.print(p);
            }
            System.out.println();
        }



        int number = 0;

        String[] duplicates = new String[5];

        String[] points = new String[] {"a", "a", "a", "b", "b", "c", "c", "c", "d", "e", "e", "f", "g", "g", "g"};

        String current = null;
        int length = 1;

        for (String point : points) {
            if (point.equals(current)) {
                length++;
            } else {
                if (length >= 3) {
                    duplicates[number] = current;
                    number++;
                }
                current = point;
                length = 1;
            }
        }

        if (length >= 3) {
            duplicates[number] = current;
            number++;
        }

        System.out.println(number);

        for (String point : duplicates) {
            System.out.println(point);
        }


        System.out.println("hello" == null);

         */
    }
}
